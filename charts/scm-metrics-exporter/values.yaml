# Default values for scm-metrics-exporter (the operator).

image:
  repository: ghcr.io/jalet/scm-metrics-exporter
  # tag defaults to .Chart.AppVersion when empty.
  tag: ""
  # digest, when set, pins the image and overrides tag.
  digest: ""
  pullPolicy: IfNotPresent
imagePullSecrets: []

# exporterImage is the image the operator injects into the exporter Deployments it
# creates. When repository is empty it reuses the operator image (single-image model).
exporterImage:
  repository: ""
  tag: ""

nameOverride: ""
fullnameOverride: ""

replicaCount: 1

leaderElection:
  enabled: true

serviceAccount:
  create: true
  name: ""
  annotations: {}

rbac:
  create: true

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    memory: 128Mi

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 65532
  seccompProfile:
    type: RuntimeDefault

securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  runAsNonRoot: true
  capabilities:
    drop:
      - ALL
  seccompProfile:
    type: RuntimeDefault

nodeSelector: {}
tolerations: []
affinity: {}
topologySpreadConstraints: []
podAnnotations: {}
priorityClassName: ""

# CRDs are templated and gated (cert-manager pattern). crds.keep adds
# helm.sh/resource-policy: keep so `helm uninstall` never deletes them (and the CRs).
crds:
  enabled: true
  keep: true

# The operator's own controller-runtime metrics endpoint. Exposed on the pod for local
# inspection; not scraped by a chart-managed Service (the product is OTLP-push only). Set
# bindAddress to "0" to disable.
metrics:
  bindAddress: ":8080"
  port: 8080

# Optional demo CR; off by default so `helm install` does not spin up collection Jobs.
sampleCR:
  enabled: false
  provider: github
  org: acme
  authMode: token
  tokenKey: token
  credentialsSecret: acme-github-credentials
  # OTLP metrics endpoint the collection Jobs push to (required).
  otlpEndpoint: http://otel-collector.observability:4318
