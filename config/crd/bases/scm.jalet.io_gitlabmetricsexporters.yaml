---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: gitlabmetricsexporters.scm.jalet.io
spec:
  group: scm.jalet.io
  names:
    kind: GitLabMetricsExporter
    listKind: GitLabMetricsExporterList
    plural: gitlabmetricsexporters
    shortNames:
    - glme
    singular: gitlabmetricsexporter
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.targetType
      name: Type
      type: string
    - jsonPath: .spec.group
      name: Group
      type: string
    - jsonPath: .spec.user
      name: User
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GitLabMetricsExporter is the Schema for the gitlabmetricsexporters
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GitLabMetricsExporterSpec configures a GitLab metrics exporter.
            properties:
              baseURL:
                description: |-
                  BaseURL is the GitLab API base URL for a self-hosted instance. Empty targets
                  gitlab.com.
                type: string
              credentialsSecret:
                description: |-
                  CredentialsSecret names the Secret in the CR's namespace holding the provider
                  credentials referenced by the provider-specific key fields.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              export:
                description: Export selects and tunes how metrics leave the exporter.
                properties:
                  exporter:
                    default: prometheus
                    description: Exporter selects the OTEL metrics exporter backend.
                    enum:
                    - prometheus
                    - otlp
                    type: string
                  otlpEndpoint:
                    description: OTLPEndpoint is the OTLP metrics endpoint, used when
                      Exporter is "otlp".
                    type: string
                type: object
              group:
                description: Group is the GitLab group to poll (targetType "group").
                type: string
              image:
                description: |-
                  Image overrides the exporter container image. When empty the operator injects
                  its own image reference.
                type: string
              pollInterval:
                default: 5m
                description: PollInterval is how often the exporter polls the provider.
                type: string
              replicas:
                default: 1
                description: Replicas is the number of exporter pods.
                format: int32
                minimum: 1
                type: integer
              resources:
                description: Resources are the exporter container's compute resources.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              serviceMonitor:
                description: |-
                  ServiceMonitor, when true, makes the operator create a Prometheus Operator
                  ServiceMonitor for the exporter (requires the ServiceMonitor CRD present).
                type: boolean
              targetType:
                default: group
                description: |-
                  TargetType selects whether Group or User is polled. User targets yield merge
                  request counts only; security findings are unavailable (GitLab vulnerabilities
                  are Ultimate/group-scoped).
                enum:
                - group
                - user
                type: string
              tokenKey:
                description: TokenKey is the CredentialsSecret key holding the GitLab
                  access token.
                minLength: 1
                type: string
              user:
                description: User is the GitLab user namespace to poll (targetType
                  "user").
                type: string
            required:
            - credentialsSecret
            - tokenKey
            type: object
            x-kubernetes-validations:
            - message: targetType 'group' requires group
              rule: self.targetType != 'group' || (has(self.group) && size(self.group)
                > 0)
            - message: targetType 'user' requires user
              rule: self.targetType != 'user' || (has(self.user) && size(self.user)
                > 0)
          status:
            description: ExporterStatus is the shared status for provider CRDs.
            properties:
              conditions:
                description: Conditions describe the current state of the exporter.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the .metadata.generation most recently
                  reconciled.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
